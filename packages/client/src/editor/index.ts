// The React editor component (Phase 12). See EditorView.tsx's own doc
// comment for scope and what remains for later phases (sentinel, cursor
// transformation under remote edits).

export { EditorView, type EditorViewProps } from "./EditorView.js";
