// Express + WebSocket gateway, coordinator, persistence — placeholder entry point.
// No feature code exists yet. Built starting the phases covering §5
// (Document Coordinator) and §11 (write path ordering) of the API Spec.

export const SERVER_PACKAGE_NAME = "@collab-editor/server";
